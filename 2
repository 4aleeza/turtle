import turtle 
t = turtle.Turtle() #obj initialized
turtle.bgcolor("black")
colors = ['light blue','blue']
t.speed(0)
for i in range(400):
    t.forward(i+2)
    t.right(89)
    t.pencolor(colors[i%2])
turtle.done()
